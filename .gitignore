*.ctx.md

# Add .turbo to your .gitignore file. The turbo CLI uses these folders for persisting logs, outputs, and other functionality.
.turbo
node_modules

# uploads in server
apps/server/uploads/*
.DS_Store
.vercel

# Environment files with real API keys (LOCAL ONLY - NEVER COMMIT)
.env.local
.env.development.local
.env.test.local
.env.production.local
apps/client/.env.local
apps/server/.env.local
apps/shared/.env.local

# Secret files
secrets/
*.secret
*.key
*.pem

# Render Configuration for BeatSync Server
app: beatsync-server
runtime: node
buildCommand: cd apps/server && bun install
startCommand: cd apps/server && bun run start
env:
  - name: NODE_ENV
    value: production
  - name: PORT
    value: "10000"
healthCheckPath: /health
autoDeploy: true

version: "2"
build:
  commands:
    - cd apps/server && npm install
start:
  command: cd apps/server && npm run start
env:
  NODE_ENV: production
healthcheck:
  path: /health
  port: 8080
networking:
  serviceName: beatsync-server
  externalPort: 443
  internalPort: 8080
